# Use hoisted node_modules layout so Metro/Babel can resolve
# presets and plugins from deep within pnpm's virtual store.
# Required for Expo + pnpm monorepos.
# See: https://docs.expo.dev/guides/monorepos/#create-a-pnpm-workspace
node-linker=hoisted
