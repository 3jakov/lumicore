import { PlaceholderRoutePage } from '@/components/layout/placeholder-route-page';

export default function TasksPage(): JSX.Element {
  return (
    <PlaceholderRoutePage
      eyebrow="Tasks"
      title="Task workspace scaffold"
      description="Prepared for list and calendar views without locking in feature UX ahead of contracts."
    />
  );
}
