import { PlaceholderRoutePage } from '@/components/layout/placeholder-route-page';

export default function CompanySettingsPage(): JSX.Element {
  return (
    <PlaceholderRoutePage
      eyebrow="Settings"
      title="Company settings scaffold"
      description="Reserved for company metadata and future working calendar settings."
    />
  );
}
